services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: poker
      POSTGRES_PASSWORD: poker
      POSTGRES_DB: poker_tournaments
    ports:
      - "5432:5432"
    volumes:
      - poker_tournaments_db:/var/lib/postgresql/data

volumes:
  poker_tournaments_db:
